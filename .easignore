# EAS Build에 업로드하지 않을 것들.
# 이 파일이 존재하면 .gitignore 대신 이 파일이 사용되므로,
# node_modules 같은 기본 제외 대상도 여기에 모두 적어야 한다.

node_modules/
.git/

# 에이전트 스킬 문서 (심볼릭 링크 — Windows에서 EPERM 발생)
.agents/
.claude/
skills-lock.json

# 설계 문서 — 빌드에 불필요
docs/

# 로컬 환경
.env.local
.env*.local
.expo/
